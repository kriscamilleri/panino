# Dark mode

Agent: Copilot CLI
Started: 2026-08-16 18:10 +02:00
Status: complete

## Objective

Add a persisted Deeply Leafy dark mode with an accessible toggle on the right side of the main
navbar.

## Progress

- Created feature branch `feature/dark-mode` from up-to-date `main`.
- Defined the user journeys, accessibility contract, palette mapping, persistence boundary, and
  security constraints in the active feature spec.
- Mapped shared Tailwind primitives, the desktop/mobile navbar, modal surfaces, and the OverType
  editor's independent color configuration.
- Added a local-storage-backed theme utility and Pinia store, then initialized the selected theme
  before the application shell mounts.
- Added the accessible theme toggle to the right side of the navbar and adapted the OverType
  editor when the preference changes.
- Added Deeply Leafy surface/token remapping for Tailwind-driven pages, dialogs, tables, inputs,
  focus rings, preview content, and editor chrome.
- Browser-verified persisted dark mode at 1280px and 375px using an isolated frontend server.

## Changes Made

- Added the active dark-mode specification.
- Added `themePreference` utility tests, `themeStore` tests, and navbar toggle interaction tests.
- Documented the root theme contract in the UI design system.

## Tests

- `npm test --prefix frontend -- --reporter=verbose tests/unit/themePreference.test.js tests/unit/themeStore.test.js`
  — 8 tests passed.
- `npm test --prefix frontend -- --reporter=verbose tests/unit/themePreference.test.js tests/unit/themeStore.test.js tests/unit/navbarThemeToggle.test.js`
  — 9 tests passed.
- `npm run test:fe` — 23 files and 364 tests passed.
- `npm run lint -- --quiet` — passed.
- `npm run build --prefix frontend` — passed.
- Browser verification at `http://127.0.0.1:5174/#/login`: persisted `data-theme="dark"`,
  Deeply Leafy body/input surfaces, and readable focused form control at 1280px and 375px.

## Open Items / Notes

- The existing shared localhost dev page had a broken Vite WebSocket, so browser verification used
  an isolated server from this branch. The unauthenticated route cannot show the workspace navbar;
  the navbar wiring and accessibility state are covered by its component test.
- Follow-up at 19:50 +02:00: replaced the original Deeply Leafy tokens with the supplied navy
  palette. The status tokens now explicitly use success `#7dff95`, warning `#ffbc5e`, danger
  `#ff8080`, and info `#87d1ff`, alongside primary `#77a0a5`, surface `#000b2b`, and accent
  `#9377a5`.
- Follow-up at 19:51 +02:00: darkened elevated and inset navy surfaces so cards, forms, and
  editor areas sit substantially closer to the `#000b2b` base.
- Follow-up at 19:52 +02:00: replaced the navy surface hierarchy with neutral off-black surfaces
  (`#0b0b0d` base, `#16161a` elevated, and `#101014` inset) while preserving the supplied
  primary, accent, and semantic colors.
- Follow-up at 19:53 +02:00: shifted the neutral surfaces toward coal (`#151515` base,
  `#242424` cards, and `#2c2c2c` inputs) and changed structural borders to charcoal `#474747`
  so hierarchy relies on surface contrast instead of blue outlines.
- Follow-up at 19:54 +02:00: softened structural borders to `#343434` and reduced foreground
  contrast to VS Code-like grays (`#d4d4d4` primary and `#a6a6a6` muted).
- Follow-up at 19:55 +02:00: introduced a lower-contrast `#272727` workspace-chrome border for
  the navbar, submenu toolbars, and document-pane separator only; cards and inputs retain their
  existing borders.
- Follow-up at 19:56 +02:00: darkened the submenu surface to `#1d1d1d`, below the `#2c2c2c`
  input inset, so Find and other toolbar fields are visually distinct.
- Follow-up at 19:58 +02:00: added a persisted desktop navbar label-collapse preference and
  changed the theme button to a text-first Dark / Light control. Mobile remains
  icon-only and does not render the desktop collapse control.
- Follow-up at 20:00 +02:00: changed the dashboard New action from the purple primary variant to
  the neutral secondary button treatment with the shared link-blue text color.
- Follow-up at 20:02 +02:00: added a dedicated New-action text class after Tailwind utilities so
  the light theme's blue link color cannot be overridden by the secondary button's gray default.
