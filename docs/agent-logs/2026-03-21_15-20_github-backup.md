# GitHub Backup

**Agent:** GitHub Copilot (GPT-5.4)
**Started:** 2026-03-21 15:20
**Status:** completed

## Objective
Implement the GitHub backup feature on a new feature branch, including backend APIs, frontend UI, daily auto-backup, and tests.

## Progress
- [x] Create feature branch for the work.
- [x] Gather backend and frontend integration points.
- [x] Implement backend GitHub backup API and storage.
- [x] Implement frontend GitHub backup UI and state.
- [x] Add automated tests.
- [x] Validate flow and finalize log.

## Changes Made
- `backend/api-service/backup.js` — added GitHub OAuth initiation/callback handling, encrypted token storage, repo selection/creation endpoints, full snapshot export, history-preserving commit flow, and daily auto-backup trigger support.
- `docs/specs/github-backup.md` — updated the feature spec from force-push semantics to history-preserving snapshot commits.
- `backend/api-service/db.js` — added non-CRR `backup_config` schema and migration helper for per-user backup settings.
- `backend/api-service/index.js` — mounted public callback and authenticated backup routes.
- `backend/api-service/sync.js` — triggered daily auto-backup checks after successful sync responses.
- `backend/api-service/tests/integration/backup.test.js` — added container-verified integration coverage for status, OAuth callback, repo listing/creation, and backup execution.
- `frontend/src/store/githubBackupStore.js` — added authenticated frontend API client/store for backup status, repo management, connect/disconnect, and manual runs.
- `frontend/src/components/GitHubBackupModal.vue` — added the GitHub Backup modal UI with connection, repository, status, and progress sections.
- `frontend/src/store/uiStore.js` — added modal visibility state/actions.
- `frontend/src/components/SubMenuBar.vue` — added the Tools → GitHub Backup entry point.
- `frontend/src/pages/HomePage.vue` — mounted the modal and handled OAuth callback query feedback.
- `frontend/src/pinia.js` — added a shared Pinia singleton so startup code and route guards can safely use stores during full-page reloads.
- `backend/api-service/backup.js` — hardened backup snapshot loading so unreferenced missing-image rows are auto-pruned, while referenced missing images return actionable note-specific errors.
- `backend/api-service/backup.js` — revised missing-image handling again so backups proceed with available content, skip referenced missing images, and persist a user-visible warning instead of failing deployment-critical backups.
- `backend/api-service/db.js` — added `last_warning` persistence for partial-backup status.
- `frontend/tests/unit/githubBackupStore.test.js` — added frontend store request-flow coverage.
- `frontend/src/components/GitHubBackupModal.vue` — now shows partial-backup warnings separately from hard failures.
- `README.md` — documented GitHub OAuth setup for production at `panino.sh` and local development, including the exact callback URL and env vars.
- `docker-compose.yml` — wired GitHub OAuth backend env vars for production containers.
- `docker-compose.dev.yml` — wired GitHub OAuth backend env vars for local development containers.

## Tests
- Ran `npm test -- tests/unit/githubBackupStore.test.js` in `frontend` — passed.
- Ran `npm run build` in `frontend` — passed.
- Re-ran `npm test -- tests/unit/githubBackupStore.test.js` in `frontend` after the Pinia bootstrap fix — passed.
- Re-ran `npm run build` in `frontend` after the Pinia bootstrap fix — passed.
- Ran `docker build -f Dockerfile.test -t panino-backup-test .` in `backend/api-service` — passed.
- Ran `docker run --rm panino-backup-test npm test -- --run tests/integration/backup.test.js` — passed.
- Re-ran `docker build -f Dockerfile.test -t panino-backup-test .` in `backend/api-service` after the missing-image handling change — passed.
- Re-ran `docker run --rm panino-backup-test npm test -- --run tests/integration/backup.test.js` after the missing-image handling change — passed.
- Re-ran `docker build -f Dockerfile.test -t panino-backup-test .` in `backend/api-service` after partial-backup warning support — passed.
- Re-ran `docker run --rm panino-backup-test npm test -- --run tests/integration/backup.test.js` after partial-backup warning support — passed.
- Re-ran `npm run build` in `frontend` after adding partial-backup warning UI — passed.
- Browser validation against `docker compose -f docker-compose.dev.yml up -d api-service frontend mailhog` — passed for authenticated Tools menu entry, modal rendering, disabled OAuth state, and mobile-width viewport check.
- Browser validation after the history-preserving copy change and Pinia bootstrap fix — passed for clean reload, authenticated startup, and updated modal wording.
- Browser validation after the missing-image handling change — passed for a live backup retry showing the new actionable error message in the modal.
- Browser validation after partial-backup warning support — passed for a live backup retry completing successfully with commit `88cee4c0e63239a25d46dc6c9722e61e198560ca` and a warning about 2 skipped missing images.
- Validated `docker compose -f docker-compose.dev.yml config` after adding GitHub OAuth env wiring — passed.
- Updated backup semantics to preserve commit history on `main` rather than force-updating it.

## Open Items / Notes
- Existing unrelated worktree changes detected in `AGENTS.md` and `docs/agent-logs/2026-03-15_18-26_branch-cleanup-review.md`; left untouched.
- Local host Node is `v24.11.1`, which is incompatible with the repo's native backend modules; backend verification was completed via the project's Node 20 Docker test image and Compose stack instead.
- GitHub OAuth remains disabled until `GITHUB_CLIENT_ID` and `GITHUB_CLIENT_SECRET` are configured for the backend environment.
- Live data diagnosis for user `42ad20ae-63ae-46d6-ae78-e29e99d15168`: note `PDF Test` currently references two image records whose files are missing on disk (`2de7bb07-2fe9-471f-9933-972f20ba5370` and `5107c9af-df74-4dca-b25f-1a89404d7aaa`). Backups now succeed by skipping those broken assets and warning the user, but the note still needs those references removed or the images re-uploaded if they should be included in future backups.

## 2026-03-21 18:30 Follow-up: Backup Progress Visibility

**Status:** completed

### Objective
Fix the GitHub backup modal so stage changes remain visible on the frontend instead of jumping from Export straight to disappearance.

### Progress
- [x] Confirm backend stage updates were being emitted correctly.
- [x] Trace the frontend polling cadence and progress panel visibility behavior.
- [x] Tighten polling and retain the final visible progress state briefly after completion.
- [x] Add unit coverage for progress-stage resolution helpers.

### Changes Made
- `frontend/src/components/GitHubBackupModal.vue` — switched backup status polling from a coarse interval to a faster single-flight loop and kept the progress panel visible briefly after completion so later stages can render.
- `frontend/src/utils/githubBackupProgress.js` — extracted progress label, step, and visible-stage helpers for the modal.
- `frontend/tests/unit/githubBackupProgress.test.js` — added unit coverage for stage label and progress-resolution behavior.
- `backend/api-service/backup.js` — removed temporary debug logging that was only used during diagnosis.

### Tests
- Ran `npm test -- tests/unit/githubBackupProgress.test.js tests/unit/githubBackupStore.test.js` in `frontend` — passed.
- Ran `npm run build` in `frontend` — passed.
- Browser validation in the live app — passed for a manual backup run showing `Building Git tree` in the modal instead of remaining on Export.
- Browser-side status trace against `/backup/github/status` during a live run — observed stages `building_tree`, `creating_commit`, and `pushing_to_github` before completion.

### Open Items / Notes
- The backend was already transitioning through all stages; the visible bug was caused by frontend timing and immediate teardown of the progress UI.
- Follow-up verification showed the status API still spent most of the run on `exporting_notes`, because `building_tree` was only published after tree entry generation finished. The stage update was moved to the start of that work so the frontend can render the active phase while the backup is still running.
- The running-job entry now remains in memory briefly after completion so the frontend can catch the final published stage before the status payload drops `isRunning` back to `false`.
- UI follow-up: repository and commit SHA values in the backup status cards now truncate with ellipses when the modal gets narrow, while preserving the full value in the native tooltip.
- Visual follow-up: the GitHub backup modal was restyled to match the other Tools modals with simpler gray headers, rectangular buttons and inputs, and neutral section chrome instead of the previous GitHub-specific treatment.
- Layout follow-up: the descriptive backup copy was moved out of the modal header and into the body above the Connection card so the header structure matches the other Tools modals.