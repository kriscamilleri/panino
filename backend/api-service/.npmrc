; Disable lifecycle scripts so native:setup can rebuild better-sqlite3 for the
; selected Node runtime before explicitly fetching Puppeteer's browser.
; CR-SQLite is a vendored prebuilt loadable extension and has no install step.
ignore-scripts=true
