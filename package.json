{
  "dependencies": {
    "minimist": "^1.2.8"
  },
  "scripts": {
    "llm": "node combine-files.cjs",
    "llmfe": "node combine-files.cjs --path ./frontend/",
    "llmtest": "node combine-files.cjs --path ./tests/",
    "llmbe": "node combine-files.cjs --path ./backend/",
    "test": "npm test --prefix tests",
    "coverage": "npm run coverage --prefix tests"
  }
}
